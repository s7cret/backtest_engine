[build-system]
requires = ["hatchling>=1.21"]
build-backend = "hatchling.build"

[project]
name = "backtest-engine"
version = "4.0.2"
description = "Independent deterministic bar-by-bar strategy backtest engine."
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "MoltBot"}]
dependencies = [
  "openpine-contracts @ git+https://github.com/s7cret/openpine-contracts.git@af9ecbc455e9af83cdc609f6b6ff85c40fb6c8bb",
]

[project.urls]
Homepage = "https://github.com/s7cret/backtest_engine"
Repository = "https://github.com/s7cret/backtest_engine"
Issues = "https://github.com/s7cret/backtest_engine/issues"

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "pytest-cov>=6",
  "ruff>=0.5",
  "mypy>=1.10",
  "build>=1.2",
  "pinelib @ git+https://github.com/s7cret/pinelib.git@801b908e0ba53d1387cfd032cb6d29aa53ba0ca0",
  "marketdata-provider @ git+https://github.com/s7cret/marketdata-provider.git@4710da99d0320e6fd1ddba6e4ae64b6d7ba68848",
]

[project.scripts]
backtest = "backtest_engine.cli.main:main"

[tool.hatch.metadata]
allow-direct-references = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.setuptools.package-data]
backtest_engine = ["py.typed"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
check_untyped_defs = false
warn_unused_ignores = false
allow_untyped_defs = true


[tool.coverage.run]
source = ["backtest_engine"]

[tool.coverage.report]
fail_under = 100
show_missing = true
